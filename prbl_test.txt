echo a faire

variable d'environnement
$? a faire

LEAKS ==========>       "> out"       "< Makefile cat "       "<< EOF"

"|" mauvais message
chaine vide avec espace qui affiche message erreur

./" qui ne fait rien ??
' seule qui segfault
" apres avoir deja fait " segfault

utiliser stat pour les cmd files 
/bin/ls

code exit des signaux
ctrl + d qui ne fonctionne pas si ecrit dans prompt
;' qui ne fait rien


--------------------------------------------------------
prompt> '
''[1]    466789 segmentation fault (core dumped)  ./minishell
--------------------------------------------------------
QUOTES SEULES


;':|}  segfault

export ne pas supprimer la value si "export TEST=bonjour" faire "export TEST" ne fait rien